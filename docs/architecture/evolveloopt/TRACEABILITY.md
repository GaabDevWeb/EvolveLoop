# Traceability

```text
Signal → Pattern → Need → Root Cause → Candidate → EvolutionRequest
  → Prototype Gate → (existing evolution pipeline)
```

IDs: `sig-*`, `pat-*`, `need-*`, `cand-*`, `ereq-*`.

Navigate: need.signal_ids / need.pattern_ids / candidate.need_id / request.source_need.
