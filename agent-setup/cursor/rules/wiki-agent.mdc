---
description: Wiki grounding + GaabType project pack map (personal overlay)
alwaysApply: true
---

# Wiki — GaabType agent grounding loop

Vault: `$WIKI_ROOT` (alias: `$RAG_REPO_ROOT`) — required; no hardcoded user path in core  
Skill: `wiki` · comando `/wiki`  
Profile: `profiles/gaabtype/`

## Project pack map (personal)

| Signal | Pack / wiki path |
|--------|------------------|
| KernelBot | `kernelbot-rag` → `KernelBot/wiki/` |
| OrbitBot | `orbitbot-kernel` → `OrbitBot/` |
| ISS / Portifolio / Xray-Spec | homonymous folders in vault |
| karpathyWiki | `.ai/` + `rag/` |

## Memory

wiki-mem hooks are **enabled** on GaabType (see `.cursor/hooks.json`).

## After code edits

Append `{Project}/log.md` in the personal vault; never rewrite `raw/`.
