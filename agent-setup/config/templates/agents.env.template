# CursorSKILLS / MegaBrain — carregado por hooks e scripts
# Edit paths for your machine after install
export AGENTS_ROOT="${AGENTS_ROOT:-{{AGENTS_ROOT}}}"
export ORCHESTRATOR_ROOT="${ORCHESTRATOR_ROOT:-$AGENTS_ROOT/orchestrator}"
export ORCHESTRATOR_JOBS_DIR="${ORCHESTRATOR_JOBS_DIR:-$ORCHESTRATOR_ROOT/jobs}"
export ORCHESTRATOR_PROMPT_DIR="${ORCHESTRATOR_PROMPT_DIR:-$AGENTS_ROOT/.cursor/pickup}"
