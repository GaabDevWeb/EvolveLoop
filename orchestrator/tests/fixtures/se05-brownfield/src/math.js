/**
 * Brownfield module — multiply exists; add is intentionally incomplete
 * until EngineeringWorker applies an ImplementationProposal.
 */
export function multiply(a, b) {
  return a * b;
}

/** Placeholder — SE-05 E2E replaces this with a real add */
export function add(a, b) {
  throw new Error("add not implemented");
}
