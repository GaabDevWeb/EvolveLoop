# planejar — Agent Package / EvolveLoop

Invocação **`/planejar`**.

1. **Ler** `.cursor/skills/planner/SKILL.md` (contrato normativo — SSOT)
2. Seguir Boundaries DO/DO NOT e Capability scope (least authority)
3. Consumir docs upstream `/prd` se existirem; respeitar HARD-GATE Fase 0.5
4. Emitir plano + `memory/<feature_id>/plan.ir.yaml` + Planner Evidence
5. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline global)
