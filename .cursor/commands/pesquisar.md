# /pesquisar — Researcher (pesquisa externa)

Invoca a skill **researcher** (`~/.cursor/skills/researcher/SKILL.md`) — specialization **Researcher** / PDA `explore`.

Alias: `/research` (mesmo package).

1. Ler `.cursor/skills/researcher/SKILL.md` (contrato normativo + `source_policy`).
2. Recolher fontes externas com tools (search/scrape/browser); **não** inventar se MCP down.
3. Grounding canónico do vault → **não** este comando; usar `/wiki`.
4. Promote / episódico → **não** este comando; usar `/mem`.
5. Coding de produto → skills de construção (backend/frontend/…).

Args opcionais após `/pesquisar`: pergunta, keywords, ou `source_policy: só docs oficiais`.
