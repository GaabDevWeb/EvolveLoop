# debugger — Agent Package / MegaBrain

Invocação **`/debugger`** (alias **`/debug`** → mesmo skill).

1. **Ler** `.cursor/skills/debugger/SKILL.md` (contrato normativo)
2. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline)
3. Capability **`debug`** — Iron Law: sem root cause, sem fix
4. Fonte Tier3: `global-skills/systematic-debugging` (DO absorvido; não duplicar agente)
5. Output: diagnóstico + fix mínimo opcional + `[ENTREGA CONSOLIDADA]`
6. Gate de suite → `/testes`; refactor estrutural → `/backend` mode `refactor`
