# database — schema & migrações

Invocação **`/database`**.

1. **Ler** `.cursor/skills/database/SKILL.md` (contrato normativo)
2. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline)
3. Inputs: `docs/DATA-MODEL.md`, `docs/API_SPEC.md` (se existirem)
4. Worker — capability `database-schema` (PDA `exec`); delegável de `backend`
