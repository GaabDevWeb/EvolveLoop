# backend — Agent Package / EvolveLoop

Invocação **`/backend`**.

1. **Ler** `.cursor/skills/backend/SKILL.md` (contrato normativo)
2. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline)
3. Worker Fase 2 — capability `backend-implementation` (PDA `exec`)
4. **Modes:** `implement` (default) | `refactor` — declarar no turno; greenfield ≠ refactor
5. Refactorer = este mode — **não** há agente `/refactorer` separado
6. Root-cause sistemático → `/debugger`
7. Output: código servidor + `[ENTREGA CONSOLIDADA]` → handoff `/testes`
