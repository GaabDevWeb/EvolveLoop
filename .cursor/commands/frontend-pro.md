# frontend-pro — Agent Package / MegaBrain

Invocação **`/frontend-pro`**.

1. **Ler** `.cursor/skills/frontend-pro/SKILL.md` (contrato normativo)
2. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline)
3. **HARD-GATE:** imagem anexada → Modo Vision + `~/.agents/skills/image-to-code/SKILL.md`
4. Capabilities: `frontend-ui` (worker) + `frontend-visual-review` (gate)
