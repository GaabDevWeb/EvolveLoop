# /research — Researcher (alias)

Alias de **`/pesquisar`**. Invoca a skill **researcher** (`~/.cursor/skills/researcher/SKILL.md`).

1. Ler SKILL + aplicar `source_policy` (citar ou `UNVERIFIED`).
2. MCP/search down → `capability_failure`; não inventar factos.
3. Vault canónico → `/wiki`; promote → `/mem`; código produto → fora de scope.
