# validar — Agent Package / MegaBrain

Invocação **`/validar`**.

1. **Ler** `.cursor/skills/po-review/SKILL.md` (aceite PO — **≠** `/code-reviewer`, **≠** `/validator`)
2. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline)
3. Gate Fase 5 — capability `po-acceptance` (subagente isolado)
