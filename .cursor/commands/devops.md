# devops — CI/CD & deploy

Invocação **`/devops`**.

1. **Ler** `.cursor/skills/devops/SKILL.md` (contrato normativo)
2. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline)
3. EvolveLoop Fase **4b** (opcional) — capability `devops-deploy` (PDA `exec`)
4. Output: pipelines, Docker, runbooks — sem código de produto
