# {{COMMAND}} — Agent Package / EvolveLoop

Invocação **`/{{COMMAND}}`**.

1. **Ler** `.cursor/skills/{{AGENT_ID}}/SKILL.md` (contrato normativo)
2. AGENTS_ROOT: `~/.cursor/agents.env` (se pipeline)
3. {{EXTRA_STEP}}
